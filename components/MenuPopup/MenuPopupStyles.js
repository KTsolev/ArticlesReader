import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    container: { position: 'absolute', top: '100%', right: 0, width: 120,
        height: 40, borderWidth: 1, borderColor: '#a8a8a8', backgroundColor: '#ededed' },
    button: { flexDirection: 'row', alignItems: 'center', justifyContent: 'space-around',
        padding: 5, width: '100%', height: '100%' } 
});