import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    container: { marginLeft: 5, flexDirection: 'row', justifyContent: 'space-between',
        padding: 5, minWidth: 50 }
});